export const postToJsonObjMapper = (postModel) => {
  //TODO: convert post ui model to request body
};

export const commentToJsonObjMapper = (commentModel) => {
  //TODO: convert comment ui model to request body
};

export const userToJsonObjMapper = (userModel) => {
  //TODO: convert user ui model to request body
};
